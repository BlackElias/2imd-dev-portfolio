 
class App {
    constructor() {
       
	
	navigator.geolocation.getCurrentPosition(getPosition);
    console.log("works")
    t
    //console.log(geolocationCoordinatesInstance.longitude);
}

 getPosition(lat, lng)
{
  
  let lat  = geolocationCoordinatesinstance.latitude;
  let lng = geolocationCoordinatesinstance.longitude;
  console.log(lat, lng)
};



   
};
let app = new App();